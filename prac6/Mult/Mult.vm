// Calculates a = x*y
// a is a local variable
// x & y are static variables

// Put your code here.
// Load x (static 0)
push static 0

// Duplicate x for comparison
push static 0

// Check if x < 0
push constant 0
lt
if-goto NEGATIVE

// If x >= 0, store x into y (static 1)
push static 0
pop static 1
goto END

// If x < 0, negate x and store in y
label NEGATIVE
push static 0
neg
pop static 1 // y = -x

label END
goto END
